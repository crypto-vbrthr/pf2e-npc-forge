{
  "name": "pf2e-npc-forge",
  "version": "0.9.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "check:release": "node scripts/dev/check-release.js",
    "check": "npm run test && npm run check:release"
  }
}
